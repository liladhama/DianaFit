// Скрипт для очистки localStorage и пересоздания программы
// Сохраните как reset-program.js и запустите через Node.js

// Создаем функцию для очистки localStorage через API браузера
function clearLocalStorage() {
  // Этот скрипт должен выполняться в браузере через консоль разработчика
  console.log('🧹 Очищаем все программы в localStorage');
  
  // Получаем все ключи, начинающиеся с program_
  const programKeys = Object.keys(localStorage).filter(key => key.startsWith('program_'));
  
  console.log(`Найдено ${programKeys.length} программ для удаления`);
  
  // Удаляем каждую программу
  programKeys.forEach(key => {
    console.log(`🗑️ Удаляем: ${key}`);
    localStorage.removeItem(key);
  });
  
  console.log('✅ Все программы очищены. Обновите страницу для создания новой программы.');
}

// Инструкции для пользователя
console.log(`
=== ИНСТРУКЦИИ ПО СБРОСУ ПРОГРАММЫ ===

1. Откройте приложение DianaFit в браузере
2. Нажмите F12 для открытия консоли разработчика
3. В консоли выполните команду:
   
   window.clearPrograms()
   
4. После подтверждения выполните:
   
   window.recreateProgram()
   
5. Страница автоматически перезагрузится
6. Проверьте, что все ползунки отображаются в центре (положение "не выбрано")

=== АЛЬТЕРНАТИВНЫЙ МЕТОД ===

1. Откройте приложение DianaFit в браузере
2. Нажмите F12 для открытия консоли разработчика
3. В консоли выполните следующий код:

localStorage.clear();
window.location.reload();

Это полностью очистит localStorage и перезагрузит страницу, после чего
приложение создаст новую программу с корректными начальными значениями.
`);
