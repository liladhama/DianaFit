import React from 'react';

const QuizSettings = ({ quizData, onSettingChange }) => {
    return (
        <div style={{
            color: '#fff',
            fontFamily: "'Alte Haas Grotesk RUS', Arial, sans-serif",
            textAlign: 'center',
            padding: '20px'
        }}>
            <h3>Настройки (в разработке)</h3>
            <p>Компонент настроек будет здесь</p>
        </div>
    );
};

export default QuizSettings;
